library project Zveno is

   for Languages use ("Ada");
   for Library_Name use "Zveno";
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Kind use "dynamic";

   package Builder is
   end Builder;

end Zveno;

